import styled from 'styled-components';

const AppWrapper = styled.div`
  display: flex;
  position: absolute;
  height: 100%;
  width: 100%;
  background: Ivory;
  color: DarkSlateGray;
`;

export default AppWrapper;
